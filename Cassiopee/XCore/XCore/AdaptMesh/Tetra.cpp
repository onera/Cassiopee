#include "Tetra.h"

const Int normalIn_T[4] = {1, 0, 1, 0};

void refine_tetra(Int tetra, Mesh *M)
{}

void reorder_tetra(Int tetra, Mesh *M)
{}

Int check_canon_tetra(Int tetra, Mesh *M)
{
    assert(0);
    return 1;
}